package com.sri.patterns.mvc.model;

public class Model {

	public int calculateAverage(int num1, int num2) {
		return (num1 + num2) / 2;
	}

	public int getMax(int num1, int num2) {
		return Math.max(num1, num2);
	}
}
